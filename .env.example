# MongoDB connection string (Atlas today; swap for Cosmos Mongo API later)
MONGODB_URI=

# Optional — defaults to "budget"
MONGODB_DB_NAME=budget

# Set to "false" only if your Cosmos account requires it
# MONGODB_RETRY_WRITES=false
